Makefile:
	Provides all options required.

Python Scripts:
	Used by Makefile

.wig files:
	Used to check the PrettyPrinter, Weeder and Compiler as a whole.

In the ./wig folder:
	/analysis
	/lexer
	/node
	/parser
	The above are generated by Sable-cc3

	/commons
	Used to build the CLI

	/compiler
	Where the Main method for starting the compilation is

	/prettyprinter
	where the pretty printing related classes are

	/weeder
	where the weeding related class is
